{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"]
    },
    "dev": {
      "inputs": ["packages/**"]
    },
    "start": {
      "inputs": ["packages/**"]
    },
    "lint": {}
  }
}
